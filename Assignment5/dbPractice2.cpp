// Program to read integers into a 3X3 matrix and display them
// Todo: Fix bugs and makesure program works as intended.
// for each bug fix, add a comments specifying what you fixed.
// Number of errors/bugs = 12


#include <iostream>

// need to use namespace std
using namespace std;
/*
// change this header to match function at bottom
void display(int Matrix[3][3], int size);

int main(void) {
	// make size a const
	int const size = 3;
	// fix matrix declaration
	int Matrix[size][size];
	cout << "Enter 9 elements of the matrix:" << endl;
	// fix for loop (move int i and change commas to semicolons)
	for (int i = 0; i < size; i++)
	{
		int j = 0;
		// fixed for loop
		for (int j = 0; j < size; j++) {
			// change colon to semicolon
			cin >> Matrix[i][j];
		}
	}
	// fix display call
	display(Matrix, 3);
	return 0;
}

// fix display parameter types
void display(int Matrix[3][3], int size) {
	// fix for loops to correctly go though the matrix
	for (int i = 0; i < size; i++) {
		for (int j = 0; j < size; j++)
		{
			// fix matrix printout
			cout << Matrix[i][j];
			if (i != size - 1 || j != size - 1)
				cout << ", ";
		}
		cout << endl;
	}
}*/
